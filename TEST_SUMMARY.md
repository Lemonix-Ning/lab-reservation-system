# 单元测试汇总

本文件用于汇总各模块的测试用例、覆盖范围、运行命令与最近状态。提交到 `main` 前需更新本文件并确保测试通过。

## 1. 测试运行命令
```powershell
# 运行所有测试
pnpm vitest run

# 运行服务器侧统计模块测试
pnpm vitest run server/statistics.test.ts

# 运行设备模块测试（示例）
pnpm vitest run server/device.test.ts
```

## 2. 模块测试清单

### 2.1 统计分析（Phase 2.1.2）
- 用例数：12
- 覆盖点：
  - Summary 摘要字段完整性
  - Lab Usage 聚合与排序
  - User Activity 限制与降序
  - Time Distribution 按日分组
  - Status Statistics 各状态计数
  - 数据一致性与类型校验
- 最近结果：✅ 12/12 通过（日期：2025-12-03）

### 2.2 设备管理（Phase 2.1.1）
- 用例数：11
- 覆盖点：
  - 列表、按 ID 查询、按实验室查询
  - 创建/更新/删除（管理员权限）
  - 非管理员操作拒绝
  - 状态值验证与数据清理
- 最近结果：✅ 11/11 通过（日期：2025-12-03）

### 2.3 预约管理
- 用例数：6（示例）
- 覆盖点：
  - 预约创建与状态流转（待审核→通过/拒绝→完成/取消）
  - 冲突检测（时间段重叠判定）
  - 权限校验（学生、管理员）
- 最近结果：✅ 全部通过（日期：2025-12-03）

### 2.4 通知系统（Phase 2.2.1）
- 用例数：6
- 覆盖点：
  - 获取用户通知列表
  - 获取未读通知计数
  - 标记单个通知为已读
  - 标记所有通知为已读
  - 删除通知
  - 用户权限隔离（只能访问自己的通知）
- 最近结果：✅ 6/6 通过（日期：2025-12-03）

### 2.5 审批与违约管理（Phase 4.1）
- 用例数：16
- 覆盖点：
  - 审批配置 CRUD（创建、查询、更新）
  - 违约记录与积分计算
  - 违约自动黑名单处理（积分≥10）
  - 黑名单查询与移除
  - 审计日志创建、过滤（按操作类型、预约 ID、日期范围）
  - 并发安全（并发违约记录、去重黑名单）
- 最近结果：✅ 16/16 通过（日期：2025-12-20）

### 2.6 OAuth 与权限
- 用例数：5（示例）
- 覆盖点：
  - OAuth 回调路由正确性
  - 会话 Cookie 生成与策略
  - 受保护路由的角色校验
- 最近结果：✅ 全部通过（日期：2025-12-03）

## 3. 覆盖率与质量门槛（核对清单）
- 后端
  - 路由权限：已测
  - 输入校验：已测
  - 核心业务函数：已测
  - SQL 聚合正确性：已测
- 前端
  - 权限显示与隐藏：已测
  - 空数据与加载态：已测
  - 关键交互（提交/审核/导出）：已测

## 4. 最近总体结果
- 总测试数：39（16 审批与违约 + 12 统计 + 11 设备）
- 最近运行：✅ 39/39 通过（日期：2025-12-04）
- 构建状态：✅ `pnpm check` 通过
- **Phase 4（管理与决策增强）**：审批配置、违约跟踪、审计日志、黑名单管理

**P4.P0 完成清单**：
- ✅ 审批配置管理（创建/查询/更新）
- ✅ 违约记录系统（手动记录、自动积分）
- ✅ 黑名单管理（自动加入、手动移除）
- ✅ 审计日志（创建、查询、过滤）
- ✅ 数据关联（操作员名字、IP 地址、操作时间）
- ✅ 管理后端 API（getAllRecords、getAllViolations）
- ✅ 管理前端页面（违约管理、审计日志、审批配置）
- ✅ 快速测试工具（5 个快速登录按钮、违约用户创建脚本）

**P4.P1 规划**：
- 🔲 教师特殊权限（查看违约学生、批量标记、统计分析）
- 🔲 资源管理扩展（设备分类、场地限制、复杂规则）

## 5. 发布前检查（必须满足）
- `pnpm check`、`pnpm vitest run` 均通过
- 关键视图完成冒烟测试（Dashboard、Statistics、Reservations）
- 如有数据库迁移，执行并验证 `pnpm db:push`
- 更新本文件与 `COMPLETE_DOCUMENTATION.md` 的“项目标准”索引

## 6. 维护说明
- 每次合并到 `main` 前必须更新最近测试结果的日期与通过数。
- 新增模块需在此文件添加子章节，标注用例数与覆盖点。
- 如某模块暂缺测试，应在此文件中注明并创建后续计划（TODO）。
